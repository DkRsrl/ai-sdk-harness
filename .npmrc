# Publish to the public npm registry. A scope mapped to a private registry in a
# user's ~/.npmrc takes precedence over publishConfig.registry, so pin the scope
# here too: a project .npmrc outranks the user one, and a publish cannot then be
# captured by whatever registry a given machine happens to map this scope to.
@dkrdev:registry=https://registry.npmjs.org/
