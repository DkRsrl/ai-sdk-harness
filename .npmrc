# This package is published to the public npm registry. Some environments map
# the @dkr scope to a private registry in ~/.npmrc; that mapping takes
# precedence over publishConfig.registry, so pin the scope here as well or a
# publish silently targets the wrong registry.
@dkr:registry=https://registry.npmjs.org/
